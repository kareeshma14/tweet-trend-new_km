pipeline {
    agent {
        node{
            label 'maven'
        }
    }

    stages {
        stage('Clone-clone') {
            steps {
                git branch: 'main', url: 'https://github.com/kareeshma14/tweet-trend-new_km.git'
            }
        }
    }
}
